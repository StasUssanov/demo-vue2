export default {
  isLoading: true,
  error: null,

  boards: {
    // id: { id, name }
  },
  lists: {
    // id: { id, board, name }
  },
  tasks: {
    // id: { id, list, title, completed }
  },
};
